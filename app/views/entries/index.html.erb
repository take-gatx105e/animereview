<% @page_title = "レビュー一覧" %>
<div class="text-center m-5">
  <h1><%= @page_title %></h1>
</div>

<section class="section">
  <div class="row">
    <% @entries.each do |entry| %>
      <%= render "entries", entry: entry %>
    <% end %>
    <%= paginate @entries %>
  </div>
</section>